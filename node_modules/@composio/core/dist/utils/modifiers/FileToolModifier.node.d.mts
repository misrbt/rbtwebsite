import { gn as Tool, xn as ToolExecuteResponse, yn as ToolExecuteParams } from "../../BaseProvider-Brb3o86-.mjs";
import ComposioClient from "@composio/client";

//#region src/utils/modifiers/FileToolModifier.node.d.ts
declare class FileToolModifier {
  private client;
  private fileUploadPathOptions;
  constructor(client: ComposioClient, fileUploadPathOptions?: FileToolModifier['fileUploadPathOptions']);
  modifyToolSchema(schema: Tool): Promise<Tool>;
  fileUploadModifier(tool: Tool, options: {
    toolSlug: string;
    toolkitSlug?: string;
    params: ToolExecuteParams;
    signal?: AbortSignal;
  }): Promise<ToolExecuteParams>;
  fileDownloadModifier(tool: Tool, options: {
    toolSlug: string;
    toolkitSlug: string;
    result: ToolExecuteResponse;
    signal?: AbortSignal;
  }): Promise<ToolExecuteResponse>;
}
//#endregion
export { FileToolModifier };